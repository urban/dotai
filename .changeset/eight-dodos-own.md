---
"@urban/dotai": patch
---

Replace custom skill-selection CLI prompts with Effect CLI Prompt-based flows, add terminal keypress support for interactive CLI usage, and support multi-select installs for interactive and argument-driven skill installation.

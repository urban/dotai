---
"@urban/dotai": minor
---

Add the first `dotai skills` workflows with installed inventory listing, source discovery for local and git-backed sources, read-only failure rendering for invalid locators, missing roots, and malformed manifests, direct skill installation with initial lock-file persistence and rollback on lock-write failure, same-source dependency planning that auto-installs hidden helper skills while preserving direct-versus-implicit lock semantics, cross-source dependency planning that resolves URL or path-based dependencies, reports unsupported or missing dependency sources with exact dependency context, blocks cyclic install graphs before mutation, CLI install UX improvements including prompted selection from operator-visible skills, the `add` alias, and explicit no-op rendering when an install request changes nothing, plus uninstall support for both blocked and successful removals through `uninstall` and `remove`, including exact dependent-skill blockers, prompt fallback that limits uninstall choices to operator-visible installed skills, lock relationship refresh, prune-candidate reporting for orphan implicit skills, rollback when uninstall lock persistence fails, and provenance-driven `update` support for full or selective refresh from recorded lock sources, including prompted root selection that excludes hidden or implicit skills and explicit recorded-source failure reporting. Standardize automated verification on Vitest plus `@effect/vitest` with a shared dotai test harness for fixture setup, Effect layer provisioning, prompt stubs, git-backed source fixtures, and repository-wide `bun run check` coverage.

Remove the incompatible legacy `@effect/platform`, `@effect/platform-bun`, and `@effect/platform-node` dependencies from the package manifest and switch the CLI entrypoint to Effect v4's unified `Runtime.makeRunMain` API so the project stays on the `effect` v4 beta line consistently.
